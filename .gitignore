__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
venv/
.venv/
node_modules/
dist/
build/
*.egg-info/
.coverage
htmlcov/
.DS_Store
*.log
.env
# Exceeds GitHub's 100MB per-file limit; regenerate via pipeline.tokenize_corpus
# (see README "Reproduce the corpus"). val.bin (2.2MB) stays committed.
data/train.bin
# Base checkpoint/weights (1.48GB / 495MB): also exceed GitHub's 100MB limit.
# metrics.jsonl (the real per-step training log) stays committed. Regenerate
# the weights with docs/GPU_RUN.md's one-command launch; runs/tiny/model.pt
# (50MB) stays committed since it fits under the limit.
runs/base/ckpt.pt
runs/base/model.pt
# base_v2 (2026-07-08 CC-100 + Wikipedia-upsampled run): same size-limit reason.
# metrics.jsonl stays committed. See README "Base model v2" for how to regenerate
# or obtain the packaged inference build.
runs/base_v2/ckpt.pt
runs/base_v2/model.pt
data_anneal/train.bin
data_anneal/val.bin
